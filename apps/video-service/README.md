# Video API Microservice (Node.js / Python)

## Description
Handles video metadata CRUD operations (titles, descriptions, URLs). Connected to PostgreSQL.
Note: Video stream `.mp4` requests bypass this API and serve directly from Object Storage (MinIO/S3).
